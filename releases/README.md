# GitHub Release notes

Copy the body of the matching `vX.Y.Z.md` file when creating a [GitHub Release](https://github.com/Makogai/session-copy/releases/new).

Pick the tag `v2.1.2` (or current version from `manifest.json`).
