# Session Copy v2.1.4

**Release packaging**

### Added
- `scripts/package-extension.ps1` and `scripts/build-store-zip.sh` for Chrome Web Store ZIP builds.
- GitHub Actions workflow uploads `session-copy-v*.zip` on each published release.
- Optional signed `.crx` when `SESSION_COPY_PEM_BASE64` repository secret is set.

**Full changelog:** https://github.com/Makogai/session-copy/blob/main/CHANGELOG.md
